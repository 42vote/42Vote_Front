export interface categoryRes {
    id: string,
    title: string,
    expired: string,
  }
  
  export interface documentListRes {
    id : string,
    title :string,
    goal : string,
    image: string,
    voteCnt : string, 
    isVoteExpired : string,
  }